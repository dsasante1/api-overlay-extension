{
  "name": "callout",
  "version": "1.0.0",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "pretest": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "jsdom": "^29.1.0",
    "typescript": "^5.4.0",
    "vitest": "^4.1.5"
  }
}
